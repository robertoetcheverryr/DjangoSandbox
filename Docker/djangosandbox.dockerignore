# Start by ignoring everything
*

# Then allow only the required directories and files
!DjangoSandbox
# In real production we should provide the .env file separately? instead of hardcoding?
!lists*
# ignore lists/static dir
lists/static
# When we move from using sqlite3 to a real DB we won't need to do migrations, I think?
!manage.py